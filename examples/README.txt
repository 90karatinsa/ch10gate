Place .ch10 and .tmats samples here for local testing.

The gate bundle script packages `sample.ch10`, `sample.tmats`, and the
contents of the `dicts/` folder to provide a minimal offline validation
set for QA demonstrations.
